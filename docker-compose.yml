version: '3.8'

services:
  postgres:
    image: postgres:16
    container_name: healthy-web-postgres
    environment:
      POSTGRES_DB: healthy_web_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"